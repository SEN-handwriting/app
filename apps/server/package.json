{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "bun --watch ./src/index.ts",
    "start": "bun ./src/index.ts",
    "build": "cd ../../packages/database && bunx prisma generate",
    "test": "echo 'Add test script here'",
    "lint": "echo 'Add lint script here'"
  },
  "dependencies": {
    "@hono/valibot-validator": "^0.5.2",
    "@repo/auth": "*",
    "@repo/database": "*",
    "@repo/validation": "*",
    "hono": "^4.10.5",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
